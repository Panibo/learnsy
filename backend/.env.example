DB_URL=mongodb+srv://<usr>:<pass>@cluster0.dzyyqy6.mongodb.net/?appName=Cluster0
MONGODB_DB=aisprint

# Local defaults; use HOST=0.0.0.0 when hosting in a container.
HOST=127.0.0.1
PORT=4000
# Comma-separated frontend origins (no paths or trailing slashes).
WEB_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
