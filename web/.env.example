# Public backend address, embedded at build time. Never put DB_URL here.
# Production defaults to https://backend-faithful-surf-4742.fly.dev.
# This override is only needed when choosing a different service.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Production defaults to /learnsy; development uses the site root.
# PAGES_BASE_PATH=/learnsy
